# 2026-04-22 N1 락사이클 체크포인트

## 현재 진행상황

- broad lane screening은 종료됐다.
- narrow scenario shortlist도 종료됐다.
- 현재 lead scenario는 `N1 Kenya dairy forage resilience`다.
- backup scenario는 `N2 India protein-security food-legume public route`다.
- `N3 East Africa public procurement dryland staple route`는 hold 상태다.
- 따라서 지금 단계는 `시나리오 종료 직전의 마지막 lock cycle`이며, 아직 crop-group long-list 단계로 넘어가지는 않았다.

## 오늘 잠근 것

- `N1 governance/legal track` 공식출처 검토를 시작했고, 기본 법·제도 decision tree를 정리했다.
- 핵심 결론은 `Kenya-sourced forage germplasm`을 쓰는 기본 경로가 자동 Treaty route가 아니라 `NEMA ABS default route`라는 점이다.
- `NACOSTI`는 연구허가와 연구 체크포인트 성격이 강하고, `KEPHIS`는 seed/plant material, quarantine, certification, plant breeders' rights 축을 맡는다.
- `ITPGRFA Annex I`는 N1 후보 forage subgroup들에 일반적으로 자동 적용되지 않는다.
- `DSI/Cali Fund`는 즉시 access permit을 대체하는 주 경로가 아니라 downstream caveat로만 붙여야 한다.

## 현재 판정

- `SG-6`는 `UNKNOWN`에서 `B`까지는 올릴 수 있다.
- 아직 `Y`로 잠그면 안 된다.
- 이유는 세 가지다.
  - exact forage subgroup가 아직 고정되지 않았다.
  - source holder가 `Kenyan local/ex situ provider`인지, `breeder-derived source`인지, `Treaty/SMTA source`인지 아직 잠기지 않았다.
  - 공식 소스끼리도 `Kenya Law의 2025 ABS 규정`과 `NEMA 운영 페이지/포털의 2006 ABS 문구`가 엇갈린다.

## 다음 액션

1. `N1 exact forage subgroup` 1개를 고정한다.
2. 그 subgroup의 `source holder`를 고정한다.
3. `breeder-derived carve-out`를 주장할지, `NEMA ABS default route`를 탈지 확정한다.
4. `named delivery owner + fallback`을 적는다.
5. `public genome saturation memo`를 붙인다.

## 단계 전환 규칙

- 위 1~5가 잠겨야 `N1 scenario close`를 선언할 수 있다.
- `N1 scenario close` 다음 단계는 `작물 분석`이 아니라 `crop-group long-list 생성`이다.
- 그 다음에야 `crop x scenario hard filter`를 돌리고, 살아남은 후보에 대해서만 deep crop analysis로 들어간다.

## 현재 판단

- 지금은 아직 `시나리오 선정 종료 직전`이다.
- 이번 cycle의 목적은 `N1을 formal close 가능한 수준으로 잠그는 것`이다.
- close가 되면 바로 `crop-group long-list` 단계로 넘어간다.
