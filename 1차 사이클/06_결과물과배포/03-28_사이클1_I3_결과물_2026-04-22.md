# I3 결과물

`cycle 1 coordinator I3`

산출물 상태: `provisional only, not final`

## Gate Completeness Audit

- `C0` 기준은 merge에 적용했다: `HF-0 / HF-3 / HF-4 / HF-7`는 pre-BWM hard gate로 유지했고, `G2`와 `G8`은 분리한 채로 audit했다.
- 입력 completeness는 `pass`다. `P1~P5` 다 수신됐고, 세 lane 모두 ledger 갱신에 필요한 최소 근거는 확보됐다.
- 다만 closure completeness는 `incomplete`다. 세 lane 모두 `pending-gate`가 hard gate 쪽에 남아 있어서, 아직 score-ready는 아니다.
- `direct official source conflict`는 이번 묶음에서는 `N1 x Chloris gayana / G6`에만 명시적으로 확인됐다.
- worker row는 substantive rewrite 없이 status carry-forward만 했다. 불충분한 곳은 `pending-gate`로 남겼다.

| Lane | Merge input | Gate coverage | Closure state | Score-ready |
|---|---|---|---|---|
| `N1 x Chloris gayana` | `P1 + P2` | 8개 컬럼 모두 채움 | `HF-0/HF-3/HF-4/G2/G5/G6/G8` 미종결, `HF-7`만 상대적으로 강함 | `no` |
| `N2 x Lens culinaris` | `P3 + P5` | 8개 컬럼 모두 채움 | `HF-0/HF-3/HF-4/G2/G4/G5/G6/G8` 미종결, `HF-7`만 종 수준 feasibility 강화 | `no` |
| `N2 x Vigna mungo` | `P4 + P5` | 8개 컬럼 모두 채움 | `HF-0/HF-3/HF-4/G2/G5/G6/G8` 미종결, `HF-7`만 종 수준 feasibility 강화 | `no` |

## Updated Lane-Level Gate Ledger

| Lane | HF-0 | HF-3 | HF-4 | HF-7 | G2 | G8 | G5 | G6 | Direct Official Source Conflict | Merge Note |
|---|---|---|---|---|---|---|---|---|---|---|
| `N1 x Chloris gayana` | `pending-gate` | `pending-gate` | `pending-gate` | `pass` | `pending-gate` | `pending-gate` | `pending-gate` | `pending-gate` | `yes` | public genome exists but benchmark insufficiency 미증명; formal seed route와 named owner는 보이지만 named payer/buyer와 acceptance criterion은 안 닫힘; legal baseline은 `NEMA ABS + NACOSTI` 쪽으로 읽히지만 운영 순서와 source-holder가 미종결 |
| `N2 x Lens culinaris` | `pending-gate` | `pending-gate` | `pending-gate` | `pass` | `pending-gate` | `pending-gate` | `pending-gate` | `pending-gate` | `none identified` | exact crop-named lentil line은 여러 개 확인됐지만 single national buyer lock은 아님; public Lens resource가 이미 두꺼워서 novelty/insufficiency burden이 큼; legal route signal은 있으나 accession/provider/public-domain이 미종결 |
| `N2 x Vigna mungo` | `pending-gate` | `pending-gate` | `pending-gate` | `pass` | `pending-gate` | `pending-gate` | `pending-gate` | `pending-gate` | `none identified` | exact urd procurement line은 확인됐지만 current end-use continuity와 national buyer closure는 안 닫힘; public Vigna mungo genome이 이미 있어서 necessity/novelty/bridge가 blocker; legal route는 Annex I 해석과 India notified status가 미종결 |

## Schema Compliance Check

- `section-level compliance`: `pass`
- `P1 / P2 / P4 / P5`는 요구된 6개 섹션을 그대로 냈다.
- `P3`도 실질적으로는 6개 섹션을 냈지만, `Bundle_ID` 섹션 안에 검색 메타와 보조 bullet이 같이 들어가 있어 형식은 가장 덜 깔끔했다.
- `FACT / INFERENCE / HYPOTHESIS / UNKNOWN` 분리는 전반적으로 잘 유지됐다. 실제 merge에는 `FACT / INFERENCE / UNKNOWN` 위주로 안전하게 반영했다.
- `G2 / G8` 분리 원칙은 유지됐다. 다만 세 lane 모두 closure는 안 됐고, 그래서 둘 다 `pending-gate`로 남겼다.
- `row-level homogeneity`: `partial`
- 이유는 bundle마다 gate row 보조 컬럼이 달랐기 때문이다. 예를 들면 `P1`은 `direct_official_source_conflict`, `P3`는 `Source_Grade/Falsifier`, `P5`는 `Lens_impact/Urd_impact/source-holder_status/public-domain_status`를 썼다.
- 따라서 raw row를 그대로 세로 결합하면 schema는 안 맞고, `I3`에서는 `status + critical blocker + conflict/search debt`만 보수적으로 normalize해서 ledger로 올렸다.
- `shared-row handling`: `pass with coordinator normalization`
- `P5`의 `N2 legal/value-capture shared` row는 status를 바꾸지 않고 `Lens/Urd` lane에 투영했다. 이건 substantive rewrite가 아니라 lane-level ledger 반영용 projection이다.

## Remaining Search Debt

- `N1 x Chloris gayana`: exact benchmark cultivar, must-win trait, threshold를 current Tolgar draft와 직접 대조하는 insufficiency memo
- `N1 x Chloris gayana`: exact material-specific source-holder, transfer chain, MTA 또는 equivalent holder proof
- `N1 x Chloris gayana`: `NEMA 선행 vs NACOSTI 선행 vs 병렬`을 닫아주는 current official sequencing note
- `N1 x Chloris gayana`: named payer / budget holder / procurement buyer / acceptance criterion
- `N2 x Lens culinaris`: current public Lens stack으로도 안 되는 exact benchmark use-case 정의
- `N2 x Lens culinaris`: program-wise sanctioned budget holder와 contracting authority를 core lentil lines별로 닫기
- `N2 x Lens culinaris`: lane-wise `G4` quantification, 즉 import dependence / intervention sensitivity를 buyer line별로 적기
- `N2 x Vigna mungo`: current urd-specific end-use continuity, 특히 historical Tamil Nadu line 이후의 최신 exact line 닫기
- `N2 x Vigna mungo`: existing draft/chromosome public genome 대비 exact novelty / insufficiency memo
- `N2 x Vigna mungo`: India notified status, accession-level provenance, MLS practicality를 official current material 기준으로 닫기
- `N2 shared`: `source-holder / public-domain status`가 안 닫혀 있어서 `G6`과 `G5` 둘 다 계속 `pending-gate`
